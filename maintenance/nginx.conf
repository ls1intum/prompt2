server {
  listen 80;

  # Serve the static maintenance assets
  root /usr/share/nginx/html;
  index index.html;

  # Always fall back to index.html so deep links do not 404
  location / {
    try_files $uri $uri/ /index.html;
  }

  error_page 500 502 503 504 /index.html;
}
