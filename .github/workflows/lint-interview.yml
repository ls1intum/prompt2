name: Lint Interview Component

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "clients/interview_component/**"

jobs:
  lint-interview:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./clients
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-lint
      - run: yarn install
      - run: yarn eslint "interview_component/**/*.{js,jsx,ts,tsx}"
