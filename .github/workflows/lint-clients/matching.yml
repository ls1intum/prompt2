name: Lint Matching Component

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "clients/matching_component/**"

jobs:
  lint-matching:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./clients
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-lint
      - run: yarn install
      - run: yarn eslint "matching_component/**/*.{js,jsx,ts,tsx}"
